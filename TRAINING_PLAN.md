# Jev 式象棋模型：下一轮训练计划

更新日期：2026-09-23。每完成一个阶段，先运行该阶段的验收，再单独 `commit` 并推送；提交包含代码、配置、指标报告和数据/权重哈希。大文件用可下载制品与哈希定位，避免无限增大 Git 仓库。阶段未达标时保留实验报告，不把候选权重替换为正式模型。

## 研究依据与边界

TypeSafe 的 [Choice 文档](https://docs.typesafe.ai/primitives/choice)定义“状态 + 封闭候选集 → 选择与完整概率分布”，[Confidence 文档](https://docs.typesafe.ai/confidence)把 confidence 定义为从分布形状导出的另一指标；[官方介绍](https://typesafe.ai/blog/introducing-system-one-models-and-jev)称其训练方向为 RLCD。目前公开材料没有足以复现 Jev 内部网络和损失函数的技术论文。本项目独立实现这个输出契约，不调用 Jev。

可复现的训练依据是 [Policy Distillation](https://arxiv.org/abs/1511.06295) 的教师到学生策略迁移、[AlphaZero](https://arxiv.org/abs/1712.01815) 的策略价值网络与自我对弈搜索、[Gumbel 搜索策略改进](https://openreview.net/pdf/4f2c0c813d0fbe127329c69b1ba216fbcd95d52c.pdf) 的小搜索预算处理，以及 [KataGo](https://arxiv.org/abs/1902.10565) 的训练样本效率改进。概率评测参考[温度缩放](https://proceedings.mlr.press/v70/guo17a.html)与 [ECE 局限性研究](https://arxiv.org/abs/1904.01685)。这些方法在象棋上的效果仍需本项目实测。

## 当前基线

2026-09-23 新增 [历史与结果标签](data/history-labels-160.jsonl)：`history-labels.js` 逐步重放 160 局棋谱，将 16,387 条教师局面与前两盘面、当前局面出现次数和终局胜负对齐。8,995 条来自有终局结果的对局，可用于真实胜/和/负标签；7,392 条未结束对局的结果保持空值。完整来源哈希与覆盖率见[核对报告](reports/history-labels-160.json)。

[5,000 局面实验](reports/choice-openings-5000.json)来自 55 局、每局面 100 ms 的 Pikafish 教师分析。新模型在排除旧数据来源后的 599 个测试局面上，教师最佳招一致率 21.0%，旧模型 16.5%；对 Pikafish 的每步 5 秒、红黑轮换 2 局得 0/2。最佳验证损失在第 2 轮出现，之后上升。校准后测试 NLL 从 2.9189 升至 2.9214，ECE 从 0.0290 升至 0.0470。以上不足以证明实战进步。

现在的教师目标把 MultiPV 分数经固定温度变成“概率”，再给最佳招额外 0.3 权重。这是启发式软标签，不是可验证的走法概率。下一轮将把**教师最佳招/排序**、**搜索访问分布**和**对局胜负**分别保存，避免混成一个标签。

## 阶段与提交门槛

| 阶段 | 工作 | 验收后提交 |
| --- | --- | --- |
| 1. 规则和评测基座（本机） | 补全长将、长捉及重复局面所需历史信息；固定不同开局、红黑轮换的对局集，记录版本、硬件、随机种子和每步 5 秒。 | 规则测试和独立局面测试通过；同一配置重跑得到一致结果；提交 `rules: ...`。 |
| 2. 教师数据 v2（本机） | 搜集公开且许可允许使用的 AI 象棋对局，并自行生成引擎对局；从开局、中局、残局和失败对局取样，先做约 20,000 局面的试验集。对少量疑难局面增加教师分析时间。保存最佳招、MultiPV 评分、深度、来源，不把评分软化结果叫校准概率。按整局及开局变化隔离训练/测试，剔除跨组重复局面。 | 棋谱逐步合法、许可/来源可追溯，覆盖率和泄漏报告通过；提交 `data: ...`。 |
| 3. 模型 v2（GPU 恢复后） | 输入加入最近局面、半回合计数与重复/长将相关状态；对全部合法走法输出 masked policy，另设胜/和/负价值头。先以教师最佳招做交叉熵、候选评分做辅助排序；有自我对弈数据后再用搜索分布训练策略、最终结果训练价值。用较小和较大网络做消融。 | 独立测试的教师选招 NLL/Top-1 与推理延迟均报告；价值头只在真实结果数据上评测；提交 `model: ...`。 |
| 4. 网络引导搜索（本机原型，GPU 加速） | 将模型先验从根节点推广到树内，批量推理；先实现 PUCT，再比较有限预算下的 Gumbel 根节点选择。完整合法招仍由规则引擎给出；搜索提议并验证，网络不得直接绕过规则。 | 固定节点数和墙钟时间下与当前 Alpha-Beta 交叉对弈；报告每步延迟、搜索节点和策略实际影响；提交 `search: ...`。 |
| 5. 自我对弈强化学习（GPU） | 保存 `(s, π_search, z)`，其中 `π_search` 是搜索访问分布，`z` 是最终胜/和/负；循环执行自我对弈、训练和候选模型擂台赛。保留旧版本对手，防止只适应自己。 | 新旧模型在固定开局成对对局中胜过基线才晋级；对 Pikafish 每步 5 秒的基准单独报告样本量与区间；提交 `selfplay: ...`。 |

## 概率的含义

`π(a|s)`首先是**搜索先验**：在给定局面和合法招中，模型认为哪步值得优先探索。用搜索访问次数训练后，它可解释为“当前搜索策略的选招分布”，仍不能解释为这步获胜的概率。胜率由单独的 WDL 头估计，并须指定对手和时间条件。

校准集只用于拟合事先确定的方法；最终测试集同时报告校准前后 NLL、多类别 Brier 分数、最高招命中率及可靠性曲线。ECE 仅作辅助观察，因为分桶选择会影响结论。只有测试集和后续新对局都显示收益时，才部署校准参数。教师仿真指标、搜索自对弈指标与实战胜率分开列示。
